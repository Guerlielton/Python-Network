name: Python Network 
on:
  workflow_dispatch:
jobs:
  docker-build:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v2
    # - uses: dopplerhq/cli-action@v1 
    # - name: set envs agent github actions
    #   run: |
    #     doppler secrets download --no-file --format=docker >> $GITHUB_ENV; 
    #   env:
    #      DOPPLER_TOKEN: ${{ secrets.DOPPLER_TOKEN_PRD }}
    # - name: Mask Vars
    #   run: echo "::add-mask::$AWS_ACCESS_KEY"
    # - uses: actions/setup-python@v2
    #   with:
    #     python-version: 3.9
    # - name: Install dependencies
    #   run: |
    #     python -m pip install --upgrade pip
    #     if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    - name: set envs agent github actions
      run: envsubst <ec2_description.py> ec2_desc.py
        env:
          AWS_ACCESS_KEY: ${{ secrets.AWS_ACCESS_KEY }}
          AWS_SECRET_KEY: ${{ secrets.AWS_SECRET_KEY }}
    - name: list
      run: cat ec2_desc.py
    - name: build docker
      run:
        docker build --build-arg "DOPPLER_TOKEN=$DOPPLER_TOKEN" -t python-network .
    